import { describe, it } from 'vitest'

// Import all test files
import './no-startswith-one-char.test'
import './no-indexof-match-zero.test'
import './duplicate-constructor.test'
import './integration.test'

describe('eslint-plugin-pere test suite', () => {
  it('should run all tests', () => {
    // This is just a placeholder to ensure all test files are loaded
    // The actual tests are in the imported files
  })
})
